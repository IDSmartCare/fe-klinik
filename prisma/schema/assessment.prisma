model MasterAssessment {
  id           Int      @id @default(autoincrement())
  idFasyankes  String
  category     String
  questionType String
  question     String
  createdBy    String
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt

  keyword AssessmentAnswer[]
}
